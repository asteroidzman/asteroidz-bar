name: aur-publish

# A pushed tag, not a published release.
#
# It used to be `release: [published]`, which meant pushing a tag published
# nothing: the AUR sat at 0.1.4 from July through 0.3.2 and 0.4.0, both of
# which were tagged. Publishing is the point of cutting a release, so the tag
# is what triggers it.
#
# Both spellings of a version tag, because the version-bump step below strips a
# leading `v` and there is no reason for the trigger to be stricter than the
# thing it triggers.
on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+'

# pushing the version-bumped PKGBUILD back to this repo needs write access;
# the default GITHUB_TOKEN is read-only for that on some repo settings.
permissions:
  contents: write

jobs:
  aur-publish:
    name: Publish asteroidz-bar to the AUR
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Bump PKGBUILD to the release version
        run: |
          VERSION="${{ github.ref_name }}"
          VERSION="${VERSION#v}"
          sed -i "s/^pkgver=.*/pkgver=${VERSION}/" PKGBUILD
          sed -i "s/^pkgrel=.*/pkgrel=1/" PKGBUILD

      # The publish comes FIRST, before anything is pushed back here.
      #
      # It used to come last, and that is one of the two reasons the AUR sat at
      # 0.1.4 for so long. `actions/checkout` on a tag -- whether the tag was
      # pushed or a release was cut from it -- checks out that tag, in detached
      # HEAD; the commit-back step then pushed that commit to `main`, which is
      # rejected outright whenever main has moved on since the tag -- and the
      # job stopped there, never reaching the step this workflow exists for. A
      # convenience commit was blocking the publish.
      #
      # The action needs nothing but the bumped PKGBUILD in the workspace, so
      # the ordering costs nothing.
      - name: Publish to the AUR
        uses: KSXGitHub/github-actions-deploy-aur@v4.2.0
        with:
          pkgname: asteroidz-bar
          pkgbuild: ./PKGBUILD
          commit_username: asteroidzman
          commit_email: asteroidzman@users.noreply.github.com
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          commit_message: Update to ${{ github.ref_name }}
          ssh_keyscan_types: rsa,ecdsa,ed25519
          # Left false, matching asteroidz. Turning it on changes what a
          # RELEASE publish does and has not been exercised; and the action's
          # default --nodeps never installs the PKGBUILD's makedepends, which
          # is what broke two of asteroidz's own publishes. Real verification
          # -- build plus the test harnesses -- happens locally before a
          # release is cut either way.
          #
          # This package would need more than --syncdeps regardless: quickshell
          # is itself an AUR package, so a plain pacman in that container
          # cannot resolve the one dependency that matters most.
          test: false

      # Housekeeping, so the PKGBUILD in this repo shows the version that was
      # published. Applied to the default BRANCH rather than pushed out of the
      # detached tag checkout, which is what used to fail.
      #
      # `continue-on-error` because this is the tidying, not the job: the AUR
      # is already updated by the time it runs, and a release cut from a tag
      # that main has since moved past must not be reported as a failed
      # publish.
      - name: Record the published version on the default branch
        continue-on-error: true
        run: |
          BRANCH="${{ github.event.repository.default_branch }}"
          VERSION="${{ github.ref_name }}"
          VERSION="${VERSION#v}"

          git config user.name "asteroidzman"
          git config user.email "asteroidzman@users.noreply.github.com"

          git fetch --quiet origin "$BRANCH"
          git checkout --quiet -B aur-bump "origin/$BRANCH"

          sed -i "s/^pkgver=.*/pkgver=${VERSION}/" PKGBUILD
          sed -i "s/^pkgrel=.*/pkgrel=1/" PKGBUILD

          git add PKGBUILD
          # Already correct, which is the normal case when the release was cut
          # from a tree that was bumped before tagging. Nothing to record.
          git diff --cached --quiet && exit 0

          git commit -m "PKGBUILD: bump to ${{ github.ref_name }}"
          git push origin "aur-bump:$BRANCH"
